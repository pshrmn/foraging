import React from 'react';
import { Link } from '@curi/react';

const AddPageControls = () => ([
  <Link key='cancel' to='Home' anchor='button' className='neg'>Cancel</Link>,
]);

export default AddPageControls;
